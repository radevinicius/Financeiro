# Database (Use DATABASE_URL OU as variáveis separadas)
DATABASE_URL=postgresql://user:password@localhost:5432/financeiro

# Ou usar variáveis separadas
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=sua_senha
PGDATABASE=financeiro
PGPORT=5432

# Server
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=seu_secret_super_seguro_aqui_com_pelo_menos_32_caracteres
